# Task List

> Generated from your PRD. Each task is one context-window unit.
> Claude picks the first unchecked task, completes it, and checks it off.
> Tasks are ordered — later tasks may depend on earlier ones.

<!--
Replace this with your app's tasks. Use the format below.
Each task should be completable in one Claude Code session.
Include specific file paths, table names, and which DOCS/CORE/ files to read.

Example:

## Phase 1: Schema & Backend

- [ ] **TASK 1: Extend schema with app tables**
  - Add `items` table with fields: projectId, title, status, createdAt
  - Add indexes: by_project
  - File: `convex/schema.ts`

- [ ] **TASK 2: Create items backend**
  - Create `convex/items.ts` with list, create, update, delete
  - Use `requireProjectAccess` for all handlers
  - Read `DOCS/CORE/ERROR_HANDLING.md` before writing

## Phase 2: App Pages

- [ ] **TASK 3: Build items list page**
  - Create `src/app/(app)/projects/[projectId]/items/` with thin page pattern
  - Read `DOCS/CORE/DESIGN_SYSTEM.md` before building
-->
